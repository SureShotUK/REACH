# Client dependencies — install on Windows 11
# Requires: Python 3.11+, PortAudio (bundled with sounddevice wheels)
sounddevice>=0.4.6
websockets>=12.0
keyboard>=0.13.5
pyperclip>=1.8.2
numpy>=1.24.0
pystray>=0.19.0
Pillow>=10.0.0
# pyaudio is not required; sounddevice handles PortAudio directly on Windows
